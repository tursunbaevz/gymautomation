<div class="forma">
  <%= bootstrap_form_for @product do |f| %>
    <% if @product.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

        <ul>
        <% @product.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

      <%= f.collection_select :category_id, Category.all, :id, :name, {:include_blank => '----'}  %>

      <%= f.text_field   :name, id: :product_name %>
      <%= f.number_field :price, id: :product_price %>
      <%= f.text_field   :p_quantity, id: :product_p_quantity %>
      <%= f.text_area    :description, id: :product_decription %>
      
      <div class="action">
        <%= f.submit %>
      </div>
  <% end %>
</div>