<div class="form-modal">
  <%= bootstrap_form_for(@sold) do |f| %>
    <% if @sold.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(sold.errors.count, "error") %> prohibited this sold from being saved:</h2>

        <ul>
        <% sold.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>


      <%= f.collection_select :product_id, Product.all, :id, :name, {:include_blank => '----'}  %>
      <%= f.date_field :sold_date, id: :sold_sold_date %>
      <%= f.text_field :sold_quantity, id: :sold_quantity %>

      <%#= f.fields_for :sold_products do |builder| %>
        <%#= render 'sold_product_fields', f: builder  %>
      <%# end %>
      
      <%#= link_to_add_fields "Выбрать продукт", f, :sold_products %> 
      
      <br>

      <div class="modal-footer">
        <%= f.submit class: 'btn btn-success' %>   
      </div>
      
      

  <% end %>
</div>