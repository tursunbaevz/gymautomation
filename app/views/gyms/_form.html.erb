<div class="forma">
  <%= bootstrap_form_for(@gym)  do |form|  %>
    <% if gym.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(gym.errors.count, "error") %> prohibited this gym from being saved:</h2>

        <ul>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>

    <div class="field">
      <%= form.text_field :title, id: :gym_title,  :class => "gym-field",placeholder: 'вид спорта' %>
    </div>  
    <div class="field">
      <%= form.number_field :g_price, id: :gym_plan, :class => "gym-field",  placeholder: 'оплата за месяц' %>
    </div>

    <div class="actions">
      <%= link_to '', gym, class: 'fa fa-list fa-fw btn btn-primary' %></td>
      <%= form.submit  class: 'btn btn-success gym-btn'%>
    </div>
  <% end %>
</div>