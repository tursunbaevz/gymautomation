<div class="forma">
  <%= bootstrap_form_for(@form_of_payment) do |form| %>
    <% if form_of_payment.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(form_of_payment.errors.count, "error") %> prohibited this form_of_payment from being saved:</h2>

        <ul>
        <% form_of_payment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.text_field :title, id: :form_of_payment_title , placeholder: 'форма оплаты...'%> 
    </div>

    <div class="actions ">
      <%= link_to '', form_of_payment, class: 'fa fa-list fa-fw btn btn-primary' %></td>
      <%= form.submit class: 'btn btn-success' %> 
    </div>
  <% end %>
</div>